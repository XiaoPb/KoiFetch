# Build context exclusions for the frontend image.
# The production bundle is built inside the image; node_modules and the local
# dist never need to cross the build context.

node_modules/
dist/

# Secrets & local env files
.env
.env.*

# Version control
.git/
.gitignore

# Runtime data (never part of the frontend)
data/
